# Example environment configuration for the chronogram pipeline
# Copy to `.env` and fill in values before running if needed

# Directory where run logs and mappings_log.xlsx are stored
CHRONO_LOG_DIR=./data/control

# API keys for AI services used by optional features
OPENAI_API_KEY=
MISTRAL_API_KEY=
